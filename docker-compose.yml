services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: usuario
      POSTGRES_PASSWORD: senha
      POSTGRES_DB: mei_no_controle
    ports:
      - "5432:5432"
    volumes:
      - dados_postgres:/var/lib/postgresql/data
volumes:
  dados_postgres:
